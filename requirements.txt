Flask
gunicorn
requests
pytz
fastkml
beautifulsoup4
shapely

# Inference, so the map can price trains itself instead of calling a second
# Render service on every 15-second poll. Two concurrent free services spend
# the account's 750 monthly instance-hours twice as fast as one, which is what
# got both suspended. scikit-learn is needed to unpickle the sklearn-API
# XGBoost models even though nothing imports it directly.
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2
