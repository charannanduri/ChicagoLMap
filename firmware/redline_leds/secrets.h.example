// Copy this file to `secrets.h` and fill in. `secrets.h` is gitignored.
#pragma once

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"

// The machine running app.py on your LAN. Use its IP, not a DNS name,
// unless your ESP32 can resolve .local / mDNS.
#define BACKEND_HOST  "192.168.1.42"
#define BACKEND_PORT  5001
